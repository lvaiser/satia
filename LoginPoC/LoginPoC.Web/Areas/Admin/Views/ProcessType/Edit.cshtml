@model LoginPoC.Model.ProcessType.ProcessType
@using LoginPoC.Model.ProcessType
@using LoginPoC.Web.Areas.Common.Models
@using Newtonsoft.Json

@{
    var jsonModel = JsonConvert.SerializeObject(Model);

    ViewBag.Title = "User Information";
}

<div ng-controller="ProcessType.Edit" ng-init='events.onInit(@Html.Raw(jsonModel))'>
    <div class="form-horizontal">
        <h4>ProcessType</h4>
        <div class="form-group">
            <div class="col-md-12">
                <label>Nombre</label>
                <input ng-model="processType.name"
                       class="form-control" type="text" name="name"
                       required
                       uib-popover="El nombre del tipo de documento es requerido" popover-trigger="'mouseenter'" "
                       placeholder="Nombre..." />

            </div>
            <div class="col-md-12">
                <label>Descripcion</label>
                <input ng-model="processType.description"
                       class="form-control" type="text" name="description"
                       required
                       uib-popover="La descripcion del tipo de documento es requerido" popover-trigger="'mouseenter'" "
                       placeholder="Nombre..." />

            </div>
            <div class="col-md-12">
                <label>Video Instructivo</label>
                <input ng-model="processType.urlVideo"
                       class="form-control" type="text" name="name"
                       placeholder="Video..." />

            </div>
            <div>
                <h4>Campos</h4>
                <div ng-repeat="field in processTypeFields track by $index">
                    <div class="col-md-12">
                        <div class="col-md-3">
                            <label>Nombre</label>
                            <input ng-model="field.name"
                                   class="form-control" type="text" name="name"
                                   required
                                   uib-popover="El nombre del campo es requerido" popover-trigger="'mouseenter'"
                                   placeholder="Nombre..." />

                        </div>
                        <div class="col-md-3">
                            <label>Tipo de dato</label>
                            <select name="fieldTypes" ng-model="field.type">
                                <option ng-repeat="type in fieldTypes" value="{{type.id}}">{{type.name}}</option>
                            </select>

                        </div>
                        <div class="col-md-2">
                            <label> <input type="checkbox" ng-model="field.isRequired" /> Es requerido</label>
                        </div>
                        <div class="col-md-1">
                            <div ng-click="events.deleteField(field);">Borrar</div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-primary pull-right" ng-click="events.newField();">Nuevo Campo</button>
            </div>
            <div>
                <h4>Documentos</h4>
                <div ng-repeat="document in processTypeDocuments track by $index">
                    <div class="col-md-12">
                        <div class="col-md-3">
                            <label>Nombre</label>
                            <input ng-model="document.name"
                                   class="form-control" type="text" name="name"
                                   required
                                   uib-popover="El nombre del documento es requerido" popover-trigger="'mouseenter'"
                                   placeholder="Nombre..." />

                        </div>
                        <div class="col-md-2">
                            <label> <input type="checkbox" ng-model="document.isRequired" /> Es requerido</label>
                        </div>
                        <div class="col-md-1">
                            <div ng-click="events.deleteDocument(document);">Borrar</div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-primary pull-right" ng-click="events.newDocument();">Nuevo Documento</button>

            </div>
            <button class="btn btn-primary pull-right" ng-click="events.onSaveClicked();">Guardar Cambios</button>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/Scripts/Site/ProcessType/ProcessType.Edit.js"></script>
}
